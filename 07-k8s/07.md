# aquaインストール。aqua.yamlはリポジトリ直下に置く
aqua init
# aquaでkindをインストール
aqua g -i  (kubernetes-sigs/kind)
# 実行
aqua i -l 
# 入ってるか試す
kind version


# ファイル作成
cluster.yaml
# # config を指定して cluster を作成
kind create cluster --config cluster.yaml

# 追加でaqua g -iを使ってaquaにインストール
kubernetes/kubernetes/kubectl
derailed/k9s

# k8s の node 一覧を表示
kubectl get nodes