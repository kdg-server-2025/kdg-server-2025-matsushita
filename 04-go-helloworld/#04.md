# historyでコマンドラインに打ったコマンドのログが見れる。
#　カレントディレクトリ（CD）　今自分がいるディレクトリのこと。

# mod
go mod init hello

# Dockerimageをビルド
docker build -t hello_04 .

# 起動
docker run --name hello_04_container hello_04


# ビルドされたイメージ確認
docker images

# 02では直接バイナリファイルを渡していて、それでもできないことはないが再現性、移植性が低いためDocker内で完結させた方が良い
