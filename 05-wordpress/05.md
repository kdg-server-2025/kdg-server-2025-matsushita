# Vscodeの拡張機能、Git Graphをインストール

# 新しい作業ブランチをVscode内（GUI）で作り、mainブランチを切った。（名前：matsushita/feat-add-03-files）

# 授業の回毎のディレクトリを作成し、作業記録のファイルを入れた。
```
mkdir 03-go-wordpress
touch 03-go-wordpress/03.md
```
# yamlファイルの作成と記述
```
cd 03-go-wordpress
touch docker-compose.yml
```
# yamlファイルに追加したimage
・mariadb
・wordpress:latest
・phpmyadmin/phpmyadmin:latest



# コンテナの生成と起動
docker-compose up -d



# 動作確認
cd 03-wordpress
docker-compose up -d (２回目から：docker-compose start)

# コンテナの停止
docker-compose stop

# -------------------------------------------------------------------
# ここからは5回目の授業の内容。指定された記載の仕方を追加したり編集したりしていく。
# latestでバージョン指定をしてはいけないので、各イメージの最新バージョンを調べていく

# 一行目のversion指定がいらないそうなので消す（今回はコメントアウトとしておく）
# volumesでDBの永続化をしたので、データが保存されるようになった。

# 間違えた物が多いので一度全てのコンテナを消しとく
docker rm -f $(docker ps -aq)

docker-compose up -d

# http://localhost:10090/ に入りアカウントを作ったのちtestという記事を作成
# コンテナの停止をする
docker-compose stop

状態を確認(docker-compose ps)
docker-compose -p 05-wordpress ps
matsushita@MachaMacBook-Air 05-wordpress % docker-compose -p 05-wordpress ps
NAME      IMAGE     COMMAND   SERVICE   CREATED   STATUS    PORTS

# 動いてない。再度起動(docker-compose start) -p は --project-nameの意味
docker-compose -p 05-wordpress start

# testという記事が残っていることを確認。成功

~~~~~~~~~~~~~~~~
tips
lsで見たとき
.今いるとこ
..今いるとこの一個上

latestとは：最新版を使いますという意味

command+f でサイト内検索

localhost←自分のPCをホストにして自分のポートにリクエストする時に使ってるやつ

一つのイメージをどうするか＝Dockerfile
複数のコンテナを管理＝Docker Compose

dockerのオプション
-d　バックグラウンドで起動する
-p　使用するポート選択
-v ボリューム
--name 生成するコンテナの名前



port設定の10090:80 の 80 は「コンテナ（ここではWordPressのApacheサーバ）が待ち受けているポート番号」のこと
~~~~~~~~~~~~~~~